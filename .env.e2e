# Loaded by Vite when the dev server is started with --mode e2e (see
# playwright.config.js's "frontend-real" project) — points the real
# storefront at the real E2E backend (backend 2.0/.env.e2e, port 5001)
# instead of the regular dev backend on port 5000. Never used for the
# mocked "chromium" project, which keeps using the plain .env file.
VITE_API_URL=http://localhost:5001
VITE_RAZORPAY_KEY_ID=rzp_test_FptB2WSdmqdoLP
VITE_SITE_URL=http://localhost:5174
